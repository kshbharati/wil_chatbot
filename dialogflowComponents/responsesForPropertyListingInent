const propertyType = request.queryResult.parammeters['propertytype'];
const location = request.queryresult.paramaters['location'];
const price = request.queryresult.paramaters['price'];

const properties = getproperties(propertytype, location, price);
if(property.length>0) {
properties.forEach((property)=>
{
response +='\n- ${property.name} ($[property.price})';
});
} else{
resposne= 'sorry';
}
